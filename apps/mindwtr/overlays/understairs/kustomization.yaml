apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../base
  - shared-config.yaml
  - httproute.yaml
  # Per-tenant backend stacks. Adding a user = add a directory under
  # tenants/, add its path to this list, done.
  - tenants/marco
