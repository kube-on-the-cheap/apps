---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: grimmory
  namespace: grimmory
spec:
  parentRefs:
    - name: external
      namespace: envoy-gateway-system
  hostnames:
    - library.homelab.blacksd.tech
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: grimmory
          port: 80
---
apiVersion: gateway.envoyproxy.io/v1alpha1
kind: SecurityPolicy
metadata:
  name: grimmory-ip-filter
  namespace: grimmory
spec:
  targetRef:
    group: gateway.networking.k8s.io
    kind: HTTPRoute
    name: grimmory
  authorization:
    defaultAction: Deny
    rules:
      - action: Allow
        principal:
          clientCIDRs:
            - 192.168.20.0/24
            - 100.64.0.0/10
